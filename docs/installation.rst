============
Installation
============

At the command line::

    $ easy_install swiftbulkuploader

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv swiftbulkuploader
    $ pip install swiftbulkuploader
